services:
  - type: web
    name: placeprep-ml-api
    env: python
    rootDir: placeprep-ml
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn api.main:app --host 0.0.0.0 --port 10000
